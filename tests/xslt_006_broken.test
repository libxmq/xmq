XML
ignoreme
XSLT
xsl:stylesheet(version   = 1.0
               xmlns:xsl = http://www.w3.org/1999/XSL/Transform)
{
    xsl:template(match = /)
    {
       // Try to generate an invalid element name. Output will be empty.
       xsl:element(name = '123{prutt}_') = Welcome
    }
}
EXPECTED
END
TARGS
CMDS